import { createTheme } from '@mui/material/styles'

export default createTheme({
  palette: {
    mode: 'dark',
  },
  typography: {
    fontFamily: 'Quicksand',
    button: {
      textTransform: 'none',
    },
  },
})
