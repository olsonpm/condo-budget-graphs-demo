import calculatedReport from '@data/calculated-report.json'
import graphDataByYear from '@data/graph.json'
import meta from '@data/meta.json'

const years = Object.keys(graphDataByYear)

export { calculatedReport, graphDataByYear, meta, years }
